{
  "name": "video-canvas",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/web"],
  "scripts": {
    "dev": "node scripts/dev-web.mjs",
    "build": "npm run build --workspace apps/web",
    "lint": "npm run lint --workspace apps/web",
    "typecheck": "npm run typecheck --workspace apps/web",
    "ui:check": "npm run ui:check --workspace apps/web"
  }
}
